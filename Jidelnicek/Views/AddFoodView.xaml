<UserControl x:Class="Jidelnicek.Views.AddFoodView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Jidelnicek.Views"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0"
                   Style="{StaticResource AddFoodTitle}"
                   Text="Přidat nové jídlo"/>
        
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal">
            <TextBlock Text="Název:"/>
            <TextBox Text="{Binding NewFood.Name, Mode=TwoWay}"
                     Background="Transparent"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal">
            <TextBlock Text="Tagy:"/>
            <TextBox Text="{Binding NewFood.Tags, Mode=TwoWay}"
                     Background="Transparent"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal">
            <TextBlock Text="Poznámky:"/>
            <TextBox Text="{Binding NewFood.Notes, Mode=TwoWay}"
                     Background="Transparent"/>
        </StackPanel>
        
        <Button Grid.Row="4"
                Content="Přidat"
                Command="{Binding AddFoodCommand}"/>
    </Grid>
</UserControl>
